apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: webapps

generatorOptions:
  disableNameSuffixHash: true

resources:
  - ../../../platform/vault/tls-base
  - service.yaml
  - ingress.yaml
